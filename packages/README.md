# Packages

Shared resources that can be consumed by multiple apps or services live here.

- `media/` – Brand and UI imagery referenced by the marketing site and chat experience.

Future packages (e.g. shared UI libraries, eslint configs, tsconfig bases) should be added under this directory.

